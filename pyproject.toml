[project]
name = "devin-dispatcher"
version = "0.3.0"
description = "Event-driven Devin API dispatcher"
requires-python = ">=3.11"
dependencies = [
    "click>=8.1",
    "httpx>=0.27",
    "jinja2>=3.1",
]
